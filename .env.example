# ============================================
# CONFIGURAÇÕES DE AMBIENTE - Fraud Detection Pipeline
# ============================================
# ⚠️  COPIE ESTE ARQUIVO PARA .env E PREENCHA COM SEUS VALORES
# cp .env.example .env
# ============================================

# ============================================
# PostgreSQL
# ============================================
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_secure_password
POSTGRES_DB=your_database_name
POSTGRES_HOST=fraud_postgres
POSTGRES_PORT=5432

# ============================================
# MinIO (Object Storage S3-compatible)
# ============================================
# Credenciais do MinIO - usadas pelo Spark para acessar o Data Lake
MINIO_ROOT_USER=your_minio_user
MINIO_ROOT_PASSWORD=your_minio_password
MINIO_ENDPOINT=http://minio:9000
MINIO_BUCKET=fraud-data

# ============================================
# Metabase
# ============================================
MB_DB_TYPE=postgres
MB_DB_DBNAME=metabase_db
MB_DB_PORT=5432
MB_DB_USER=your_postgres_user
MB_DB_PASS=your_secure_password
MB_DB_HOST=postgres
MB_SITE_URL=https://your-domain.com

# ============================================
# Kafka
# ============================================
KAFKA_BROKER=kafka:9092
KAFKA_TOPIC=transactions

# ============================================
# Spark
# ============================================
SPARK_MASTER=spark://spark-master:7077

# ============================================
# NOTAS DE SEGURANÇA
# ============================================
# 1. NUNCA commite o arquivo .env no Git (já está no .gitignore)
# 2. As credenciais do MinIO são injetadas automaticamente nos 
#    containers Spark via docker-compose.yml
# 3. O config.py lê essas variáveis e configura o Spark
# 4. Seus scripts Python ficam limpos, sem senhas hardcoded
